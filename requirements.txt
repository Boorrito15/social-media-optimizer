# Data & Ingestion
yt-dlp
opencv-python-headless
pandas
polars
pyarrow

# GCS uploads (preferred transport; works with just the service-account key)
google-cloud-storage

# Video / ML pipeline
#  NOTE: ffmpeg must also be installed on the host (brew/apt) for the 480p
#  transcode step. It is used via utils/ffmpeg.py.
# google-cloud-storage   # optional: alternative to the gsutil CLI for uploads

# ML & NLP
torch
google-genai
openai-whisper
xgboost
scikit-learn
sentence-transformers    # used by models.shared.clustering (predict-time embeddings)
regex                    # Unicode-aware patterns for hashtag/mention/emoji multi-hot

# UI & Visualization
streamlit
plotly

# Dev Tools
python-dotenv
pytest
